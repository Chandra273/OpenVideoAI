# Database URL (Postgres)
DATABASE_URL=postgresql://postgres:password@localhost:5432/openvideoai

# JWT secret key
SECRET_KEY=change-me

# App settings
APP_ENV=development
# D-ID API key (optional). Set this to enable external actor-synthesis provider.
DID_API_KEY=
